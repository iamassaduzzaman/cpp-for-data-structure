#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s = "hi mom";

    // cout << s[0] << endl; -> access the ith index of the string
    // cout << s.at(1) << endl; -> access the ith index of the string
    // cout << s.back() << endl; -> access the last element of the string
    // cout << s.front() << endl; -> acess the first element of the string
    return 0;
}