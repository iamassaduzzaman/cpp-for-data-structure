#include <bits/stdc++.h>
using namespace std;

int main()
{
    string a = "hi mom";
    string b = ", wassup?";

    // a +=b; -> append another string
    // a.append(b); -> append another string
    // a.push_back('m'); -> add character to the last of the string
    // a.pop_back(); -> remove the last character of the string
    // a = "hello mom"; -> assign string
    // a.assign("hello mom"); -> assign string
    // a.erase(2, 1); ->  erase character form the string
    // a.replace(2, 2, "hi"); -> replace a portion of the string


    cout << a << endl;
    return 0;
}